endpoint: https://lambda-swaap-d65ae6a665.herokuapp.com/swaap/${env:SERVICE_STAGE}
datamodel: datamodel.prisma

generate:
  - generator: javascript-client
    output: ./generated/prisma-client/

hooks:
  post-deploy:
    - prisma generate