const Query = require('./query');
const Mutation = require('./mutation');
const User = require('./user');
const Conference = require('./conference');

module.exports = [Query, Mutation, User, Conference];
